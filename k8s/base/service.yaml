apiVersion: v1
kind: Service
metadata:
  name: domain-manager
  namespace: domain-manager
  labels:
    app: domain-manager
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: domain-manager

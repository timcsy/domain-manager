apiVersion: v1
kind: Service
metadata:
  name: {{ include "domain-manager.fullname" . }}
  labels:
    {{- include "domain-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domain-manager.selectorLabels" . | nindent 4 }}
